<UserControl x:Class="PubgConnect.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="440">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Style="{StaticResource GlassCardStyle}" Width="400" Padding="32,28">
            <StackPanel>
                <!-- App Logo & Header -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,24">
                    <TextBlock Text="🎮" FontSize="42" HorizontalAlignment="Center" Margin="0,0,0,8" />
                    <TextBlock Text="PUBG Connect" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="GameLoop Friend Status Notifier" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Email Input -->
                <TextBlock Text="Email Address" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>

                <!-- Password Input -->
                <TextBlock Text="Password" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" FontWeight="Medium" Margin="0,0,0,6"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,16" PasswordChanged="PasswordBox_PasswordChanged"/>

                <!-- Server Settings Toggle -->
                <Button Content="⚙️ Server Connection (Internet / LAN)" Command="{Binding ToggleServerSettingsCommand}"
                        Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextSecondaryBrush}" 
                        FontSize="11" Cursor="Hand" HorizontalAlignment="Right" Margin="0,0,0,12"/>

                <!-- Expandable Server URL Input -->
                <Border Background="#0F172A" BorderBrush="{StaticResource BorderBrushDark}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,16"
                        Visibility="{Binding ShowServerSettings, Converter={StaticResource BoolToVisConverter}}">
                    <StackPanel>
                        <TextBlock Text="Server Host / IP Address:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" FontSize="12" Height="36" Padding="8,6"/>
                        <TextBlock Text="e.g. http://192.168.1.50:5000 or https://your-server.com" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentRedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,12"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVisConverter}}"/>

                <!-- Login Button -->
                <Button Content="Log In" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="44" Margin="0,0,0,16"/>

                <!-- Divider & Register Link -->
                <Separator Background="{StaticResource BorderBrushDark}" Margin="0,8,0,16"/>

                <Button Content="Don't have an account? Create Account" Command="{Binding GoToRegisterCommand}" 
                        Background="Transparent" BorderThickness="0" Foreground="{StaticResource AccentBlueBrush}" 
                        FontSize="13" Cursor="Hand" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
